/**
 * Intentionally empty — dynamic CMS images come from Admin API / Cloudinary only.
 * Kept as a module so older imports do not break during cleanup.
 */
export const PREMIUM_VISUALS = {} as const;
